#pick a random number between 1 and 30
#step1(inform user that computer has selected a number between 1 and 30)
#step inform user that they have to guess the number
import random
number = random.randint(1,30)
input( "enter a no 1,30 that you think the computer has selected")